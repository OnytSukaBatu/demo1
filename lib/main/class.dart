import 'package:flutter/material.dart';

class Ca {
  static CrossAxisAlignment baseline = CrossAxisAlignment.baseline;
  static CrossAxisAlignment center = CrossAxisAlignment.center;
  static CrossAxisAlignment end = CrossAxisAlignment.end;
  static CrossAxisAlignment start = CrossAxisAlignment.start;
  static CrossAxisAlignment stretch = CrossAxisAlignment.stretch;
}

class Ma {
  static MainAxisAlignment center = MainAxisAlignment.center;
  static MainAxisAlignment end = MainAxisAlignment.end;
  static MainAxisAlignment around = MainAxisAlignment.spaceAround;
  static MainAxisAlignment between = MainAxisAlignment.spaceBetween;
  static MainAxisAlignment evenly = MainAxisAlignment.spaceEvenly;
  static MainAxisAlignment start = MainAxisAlignment.start;
}

class Ms {
  static MainAxisSize max = MainAxisSize.max;
  static MainAxisSize min = MainAxisSize.min;
}
